https://www.w3schools.com/quiztest/quiztest.asp?qtest=GIT

1. git commit
takes the staged snapshot and commits it to the project history. 
2. git init
initializes a new Git repository. 
3. git diff
diﬀerence of what is changed but not staged
4. git log
show all commits in the current branch’s history
5. git add --all
adds all files and changes of the current folder to the repository

